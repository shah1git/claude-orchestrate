# /orchestrate — оркестрация задач по моделям

Глобальный скилл Claude Code: основная сессия (Fable или Opus) выступает
оркестратором — декомпозирует задачу, раздаёт подзадачи агентам на разных моделях,
проверяет качество и синтезирует результат. Модели воркеров закреплены во фронтматтере
агентов и от модели-лидера не зависят. По умолчанию самодостаточен и работает целиком
внутри Claude Code, в рамках подписки; опционально — кросс-провайдерный слой (Codex,
Gemini/Antigravity), зажигаемый только при наличии коннектора.

## Состав

| Файл | Назначение |
|---|---|
| `SKILL.md` | Плейбук оркестратора: триаж → уточнение → план → одобрение → делегирование → проверка → синтез |
| `references/delegation.md` | Шаблон задачи-«тикета», примеры, шпаргалка промптинга под каждую модель |
| `references/quality.md` | Метрики качества: критерии приёмки, иерархия проверок, схема вердикта, скоркарта, телеметрия маршрутизации (§7) |
| `references/clarify.md` | Грилл-гейт уточнения запроса (Step 0.5): метод, ledger, политика записи артефактов, форматы CONTEXT.md/ADR; адаптация grill-with-docs |
| `references/cross-provider.md` | Опциональный кросс-провайдерный слой (v2.0): реестр коннекторов, детекция/деградация, 3 применения (кросс-модельная линза, Gemini-разведка, Codex-кодинг) |
| `telemetry/routing-log.jsonl` | Журнал маршрутизации: по JSONL-записи на каждый делегированный тикет; данные для перекалибровки рубрики Step 2 |
| `~/.claude/agents/architect.md` | Fable, effort xhigh — проектирование, сложный анализ, отладка |
| `~/.claude/agents/builder.md` | Sonnet, effort high — реализация по спецификации |
| `~/.claude/agents/scout.md` | Haiku — механическая разведка; структурно read-only (только Grep/Glob/Read, без shell) |
| `~/.claude/agents/critic.md` | Opus, effort xhigh — адверсариальная проверка, свежий контекст |

Модели указаны семействами, без номеров версий: во фронтматтере агентов стоят
плавающие псевдонимы (`fable`, `opus`, `sonnet`, `haiku`), и каждый запуск автоматически
использует новейшее поколение семейства. Версионно-специфичные заметки о поведении
моделей собраны в `references/delegation.md` под явным баннером «Verified for …»
с правилом перепроверки при смене поколения.

## Использование

- `/orchestrate <задача>` — явный запуск.
- Автоматически: Claude сам подхватывает скилл на крупных многочастных задачах
  (описание скилла содержит триггер-фразы «оркеструй», «раздай задачи» и т. п.).
- Агентов можно звать и напрямую, без скилла: «спроси у architect…»,
  «пусть critic проверит…».

## Ключевые решения

- **Haiku — да, но узко**: только механические, однозначные, read-only тикеты с точным
  форматом вывода и стоп-правилом `NEEDS_CLARIFICATION` вместо догадок. Экономит квоту
  Opus/Fable; любая задача с суждением уходит на Sonnet. Read-only обеспечен
  структурно — у scout нет Bash и инструментов записи, а не только запретом в промпте.
- **Дисциплина рабочего дерева**: параллельные builder'ы пишут только в
  непересекающиеся пути; полные прогоны тестов — после завершения волны (у critic);
  критерии «diff только в разрешённых путях» проверяются по пофайловому снимку diff,
  а не по глобальному git status.
- **Производитель никогда не проверяет сам себя**: critic получает только результат и
  критерии приёмки, без рассуждений производителя (рекомендация Anthropic —
  fresh-context refutation).
- **Сложное — никогда вниз**: классы подзадач (суждение / исполнение по спецификации /
  механика) с жёсткими полами — архитектура, root-cause, безопасность, финальная
  проверка кода и содержательные линзы аудита никогда не опускаются ниже Opus, а два
  маршрута с v2.3 идут выше: архитектура/root-cause — на Fable-architect, линза
  корректности высших ставок — на Fable-critic. При сомнении между уровнями — маршрут
  вверх: промах вверх стоит квоты, промах вниз — тихой ошибки. Эскалация односторонняя:
  провалившаяся на уровне подзадача вниз не возвращается.
- **Эскалация ограничена**: FAIL → один повтор с замечаниями → подъём уровня модели
  (haiku → sonnet → opus → fable-воркер в свежем контексте → лид) → доклад о блокере.
  Не более двух попыток на подзадачу. Ступень fable-воркера существует потому, что
  «лид сделает сам» — не свежий взгляд: к этому моменту контекст лида уже несёт все
  провалившиеся попытки.
- **Рубрика калибруется данными**: каждый делегированный тикет фиксируется в
  `telemetry/routing-log.jsonl` (класс, маршрут, вердикт с первой попытки, эскалации);
  пороги пересмотра рубрики — quality.md §7. Маршрутизация перестаёт быть навсегда
  априорной.
- **Уточнение до декомпозиции**: перед дорогой волной лид проводит грилл-гейт (Step 0.5,
  references/clarify.md) — сократический допрос по одному вопросу за раз, глубина по триажу
  (таблица «тир × неоднозначность»), число вопросов — суждение агента, а не квота; артефакты
  CONTEXT.md/ADR по умолчанию не пишутся. Плюс чекпойнт одобрения плана до первого воркера.
- **Аудит связующей ткани**: critic для сводок сверяет с источником кванторы и
  причинно-временные связки («всегда», «поэтому», «после») — там, где галлюцинируют сводки.
- **Кросс-провайдер — опционально, две поверхности, с определением доступности**: лид может
  звать не-Claude-модели (Codex, Gemini/Antigravity) — но только по одной из трёх названных
  причин (независимая линза проверки; контекст больше окна Claude; явный выбор Codex-кодинга —
  опция, дефолтный кодер остаётся Claude-builder) и только если поверхность найдена; иначе —
  тихий откат на Claude-воркера. Две поверхности: **MCP** для интерактива и **мост
  `agent-bridge`** (внешний проект `/opt/tools/agent-bridge`, обоснование — его ADR-0001) для
  структурного, машинно-проверяемого вердикта — потому что у Codex `--output-schema` молчит под
  MCP. Мост **ссылаем, не вендорим**. Путь «только Claude» остаётся дефолтом и самодостаточен;
  «производитель ≠ проверяющий» выходит сам собой.
- **Плейбук учится на сбоях**: журнал инцидентов (quality.md §8) — качественная пара к
  количественной телеметрии §7; 2+ сбоя одного класса → правка правила, а не симптома.
  Плюс дисциплина спеки: проверка обязана уметь упасть (N1), никаких hedge-слов в тикете
  (N2), scout строго «глаза, не голова» — по списку глаголов (N3).
- **Fable — там, где концентрируется суждение, а не масса вызовов**: `architect` закреплён
  за Fable (проектирование, root-cause, риски — ~1 вызов на прогон, стоимость ограничена;
  тикет пишется «цель, а не шаги»), и линза *корректности* при двойной проверке высших
  ставок идёт на Fable (`critic` c `model: fable`) — две линзы на двух моделях декоррелируют
  отказы самого дорогого гейта. `builder` сознательно остаётся Sonnet: самая массовая роль,
  буквальное исполнение спеки — там буквальность достоинство, а фронтирная квота — расточительство.
- Скилл и агенты — англоязычные (промпты моделям), отчёт пользователю — на его языке.

## Источники (проверены 2026-07-03)

- anthropic.com/engineering/building-effective-agents — паттерны, «начинай с простого»
- anthropic.com/engineering/multi-agent-research-system — анатомия делегирования,
  масштабирование усилий, рубрика LLM-judge
- platform.claude.com/docs — prompting best practices; страницы промптинга
  Opus 4.8 / Sonnet 5; choosing-a-model; критерии успеха и evals
- code.claude.com/docs — skills, sub-agents, best practices (verification)
- github.com/mattpocock/skills — grill-with-docs (MIT, Matt Pocock): метод грилла,
  форматы CONTEXT.md/ADR (адаптировано в references/clarify.md)

## Changelog

- 2026-07-08 — v2.3: Fable в матрице ролей — «максимум без фанатизма». По решению
  пользователя: (1) `architect` переведён с Opus на Fable (фронтматтер `model: fable`,
  effort xhigh) — роль с открытым пространством решений и низкой частотой вызовов, ровно
  профиль фронтирной модели; шпаргалка промптинга разделена: architect-тикет пишется
  Fable-стилем («цель, а не шаги» — гиперскриптованная процедура вредит), critic-тикет —
  Opus-стилем (буквальность). (2) Линза корректности при двойной проверке высших ставок
  закреплена за Fable (`critic` + `model: fable`); линза полноты остаётся на Opus — две
  модели декоррелируют отказы гейта. (3) Потолок up-routing переформулирован: у
  `model: fable` ровно три санкционированных применения (architect по конструкции; линза
  корректности высших ставок; финальная ступень эскалации) — builder/scout не берут его
  никогда. `builder` остаётся Sonnet, `scout` — Haiku: «примитивное — дешёвым» не тронуто.
  Исправлена и предпосылка запроса: билдер и до этого был Sonnet, а не Opus.
- 2026-07-06 — v2.2: кросс-провайдерный слой переориентирован на мост `agent-bridge`
  (`/opt/tools/agent-bridge`) — существующий проект пользователя, который мы **ссылаем, а не
  вендорим**. Две поверхности вместо «только MCP»: MCP (`codex mcp-server`, `gemini-mcp-tool`)
  для интерактивного «второго мнения»; мост (`run-external-agent.mjs`: `codex exec
  --output-schema`, `agy --print --model`) — для структурного вердикта, потому что у Codex
  `--output-schema` молча игнорируется под MCP (codex #15451). Исправлены факты по Gemini,
  проверенные вживую (тест 2026-07-06 + `agy models`, agy 1.0.16): выбор модели через `agy`
  работает (мой прежний тезис «заперт на Flash» относился к MCP-обёртке, не к agy); эффорт
  Gemini — в суффиксе имени `(High/Medium/Low)`; критика — `Gemini 3.1 Pro (High)` (3.5 Pro
  **не в паблике**, из пинов убран, как и несуществующий здесь flash-lite); разведка —
  `Gemini 3.5 Flash (High)`, дёшево-массово — `(Low)`. Новая дисциплина: **пиновать только то,
  что реально отдаёт `agy models`/`codex`, а не веб-анонсы.** Токены: через мост есть
  `durationMs` и (у Codex) счётчик в `stderrTail`; у MCP-пути — нет, там `n/a`. ADR-0001 —
  источник обоснования (не дублируем); pal/clink помечен как оценён-и-отклонён (резерв).
- 2026-07-05 — v2.1: шесть облегчённых правил из сравнения с fable-ruki-agenty + карточка
  агента. (N1) Фальсифицируемый DoD — проверка обязана уметь упасть на сломанной работе
  (quality.md §1). (N2) Запрет hedge-слов в тикете («вероятно/видимо» = недоразведка, не
  спека) — delegation.md. (N3) Бело/чёрный список глаголов scout (найди/сверь — да;
  выбери/реши/сделай вывод — нет; выбор из N → «все N с атрибутами, решает лид») —
  scout.md + delegation.md. (N4) Журнал инцидентов — качественное дополнение к телеметрии
  §7: «2+ сбоя одного класса → правка плейбука» (quality.md §8, опциональный
  telemetry/incidents.md). (N5) Сверка предзаготовленной спеки с фактическим диффом перед
  выдачей (SKILL.md Step 3). (N6) Финальное адверсариальное ревью всего интегрированного
  диффа после многописательской волны — ловит дефекты шва, которые пропускает пофайловая
  проверка (SKILL.md Step 4). Плюс: лид показывает компактную карточку по каждому
  вернувшемуся агенту (модель, токены, исход, проблемы, Notable), а не только в финале
  (SKILL.md Step 5). Всё — аддитивные правки; шаги не перенумерованы (кроме нового пункта 7
  в Step 4).
- 2026-07-05 — v2.0: опциональный кросс-провайдерный слой (references/cross-provider.md).
  Лид может напрямую через MCP делегировать не-Claude-моделям — OpenAI Codex и Google
  Gemini (через Antigravity CLI `agy`) — но это опция, а не дефолт: путь «только Claude»
  остаётся самодостаточным, кросс-провайдер зажигается лишь при наличии коннектора и лишь по
  одной из трёх названных причин (независимая линза проверки; контекст больше окна Claude;
  явный выбор Codex-кодинга — по решению пользователя ОПЦИЯ, дефолтный кодер остаётся
  Claude-builder). Три применения: (1) кросс-модельная критика — добавочная третья линза,
  только ужесточает гейт; (2) Gemini на большой контекст — read-only scout с ограниченным
  выводом; (3) Codex-кодинг — пишет только в worktree (workspace-write, никогда
  danger-full-access), диф читается с диска и проверяется не-Codex-критиком. Не-Claude-
  воркер — не суб-агент, а MCP-вызов из главного цикла; коннекторы заданы реестром (Gemini:
  agy → легаси gemini-cli), что переживает миграцию. Телеметрия §7 получила опциональные
  поля provider / xprovider_reason. Модель и reasoning-эффорт пинуются по каждому применению
  (критика — xhigh, кодинг — high; §Model & effort pins): эффорт задаётся явно, а модель
  «плавает» к текущему топу коннектора (Codex дефолт `gpt-5.5` → `gpt-5.6 Sol` при GA;
  превью с 26.06.2026) под баннером «verified-for» — по образцу нашей политики «семейство,
  не версия». Gemini-тиры: разведка по умолчанию — `gemini-3.5-flash` (обгоняет 3.1 Pro,
  дешевле и быстрее, 1M-контекст), высокообъёмная механика — `gemini-3.1-flash-lite`
  (Haiku-lane), кросс-критика — `gemini-3.5-pro`; модель для Gemini передаётся явно —
  дефолт коннектора отстаёт (в тесте вернул 3.1 Pro). install.sh не менялся; агентских
  файлов не добавлено.
- 2026-07-05 — v1.8: «отчёт агента по факту» — лёгкое расширение существующего.
  (1) Опциональные поля стоимости в журнале телеметрии (quality.md §7): `tokens` /
  `tool_uses` / `duration_ms` — харнесс отдаёт их в блоке `usage` каждого результата Agent,
  лид уже их видит; позволяют калибровать стоимость-на-класс, а не только корректность.
  (2) Поле «Notable beyond the ticket» в формате отчёта architect / builder / critic —
  институционализирует находки вне рамок тикета; scout сознательно освобождён (структурно
  «только факты, без суждений» — у него для этого поле GAPS). (3) Скоркарта (§5) получила
  строку стоимости по агентам, а раздел «Notable findings» в Step 5 собирает поля Notable
  воркеров. Тяжёлую версию (персистентный файл-отчёт на агента, шина Mahler #12) сознательно
  не берём — конфликтует с «вся координация через лида».
- 2026-07-05 — v1.7: три возможности по итогам сравнения с оркестратором Mahler.
  (1) Грилл-гейт уточнения запроса перед декомпозицией (Step 0.5, references/clarify.md) —
  адаптация grill-with-docs (Matt Pocock, MIT): сократическая сверка по одному открытому
  вопросу за раз, заострение терминологии, сверка утверждений с кодом; глубина
  масштабируется по триажу (таблица «тир × неоднозначность»), число вопросов определяет
  агент по условию остановки, а не фиксированная квота; артефакты CONTEXT.md/ADR по
  умолчанию не пишутся (политика on/offered/off), «ledger» уточнений питает декомпозицию и
  поля тикетов. Метод втянут внутрь (свой references/clarify.md с атрибуцией), внешний скилл
  в рантайме не вызывается — orchestrate остаётся самодостаточным. (2) Чекпойнт одобрения
  плана до первой волны (закрытие Step 1) — для дорогих/высокоставочных прогонов лид
  компактно показывает план маршрутизации и ждёт явного одобрения; поведенческий гейт, не
  зависящий от внутренностей plan mode. (3) Аудит «связующей ткани» в методе critic и
  рубрике quality.md §4 — кванторы и причинно-временные связки сверяются с источником для
  сводных деливераблов, где концентрируются галлюцинации. install.sh не менялся — clarify.md
  ставится в составе каталога skill/orchestrate.
- 2026-07-04 — v1.6: пять улучшений по итогам самоаудита. (1) Постоянная телеметрия
  маршрутизации — `telemetry/routing-log.jsonl`, по JSONL-записи на делегированный
  тикет; схема и пороги перекалибровки рубрики — quality.md §7. (2) Детерминированные
  проверки — обязательный предгейт: critic запускается только после их прохождения,
  провал на механике возвращается производителю без затрат токенов Opus. (3) В лестницу
  эскалации добавлена ступень fable-воркера (свежий контекст) между Opus и лидом;
  задокументирован потолок up-routing — `model: fable` только для финальной эскалации
  или самой тяжёлой линзы суждения. (4) Волны записи от трёх параллельных builder'ов —
  только в worktree-изоляции (структурное устранение класса ошибок вместо правил
  против него); до двух писателей на доказуемо непересекающихся путях — прежняя
  дисциплина общего чекаута. (5) Двойная линза critic (корректность + полнота двумя
  параллельными агентами) для деливераблов высшей ставки — лид применяет её
  автоматически по триггер-условиям (продуктовый код в main, внешние публикации,
  результаты, принимаемые без чтения); пользователю не нужно помнить об опции.
  Запланировано, не внедрено: эксперимент «scout против встроенного Explore» на
  первой реальной разведочной задаче; эксперимент с режимом agent teams
  (экспериментальный флаг Claude Code) как третьим каналом делегирования для узкой
  ниши — длинные многоволновые реализации и исследования с конкурирующими
  гипотезами — при двух жёстких правилах: critic всегда вне команды (одноразовый,
  свежий контекст) и назначение задач только лидом, без самозахвата из общего
  тасклиста.
- 2026-07-04 — v1.5: версии моделей убраны из таблиц маршрутизации и ярлыков — уровни
  обозначаются семействами (Opus / Sonnet / Haiku), а привязку выполняют плавающие
  алиасы во фронтматтере агентов, всегда разрешающиеся в новейшее поколение.
  Версионно-специфичные поведенческие заметки собраны в references/delegation.md под
  явным баннером «Verified for: Opus 4.8 · Sonnet 5 · Haiku 4.5 · Fable 5» с правилом
  перепроверки при выходе нового поколения: расхождение между текстом плейбука и
  фактически запускаемой моделью теперь видимо, а не молчаливо. Исторические
  упоминания версий (changelog, список источников) сохранены намеренно.
- 2026-07-04 — v1.4: `effort: xhigh` во фронтматтере скилла — триаж, декомпозиция и
  роутинг у лида гарантированно идут на максимальной глубине рассуждения независимо от
  сессионного effort (пины воркеров были заданы раньше: architect/critic xhigh,
  builder high, scout — параметр не поддерживается Haiku).
- 2026-07-04 — v1.3.1: плейбук сделан независимым от модели-лидера — оркестратором
  может быть Fable или Opus; модели воркеров прибиты во фронтматтере и не наследуются;
  для Opus-лидера добавлено напоминание о его склонности недоделегировать.
- 2026-07-04 — v1.3: детерминированный роутинг по сложности. Рубрика «класс подзадачи →
  модель» (суждение → Opus, исполнение по спецификации → Sonnet, механика → Haiku);
  правило «при сомнении — вверх» (стоимость ошибок асимметрична); жёсткие полы —
  архитектура, root-cause, безопасность, финальная проверка кода, содержательные линзы
  аудита никогда не опускаются ниже Opus; смешанный класс — делить или брать высший;
  эскалация односторонняя; обязательный письменный routing-план с обоснованием каждого
  маршрута ниже Opus; ужесточены условия downgrade для critic (три условия
  одновременно, «экономия квоты» — не основание).
- 2026-07-04 — v1.2: обязательная маршрутизация в Workflow-сценариях — обнаружено на
  практике: `agent()` без `agentType`/`model` наследует модель сессии (Fable), и совет
  самого инструмента «model по умолчанию не указывать» ломал матрицу маршрутизации.
  Теперь каждый `agent()` обязан нести `agentType` (предпочтительно) или явный `model`;
  сценарий с «голыми» вызовами не запускается. Плюс фикс install.sh по вердикту critic:
  `--copy` теперь бэкапит существующую несимлинк-цель в `.bak`, а не удаляет её молча.
- 2026-07-04 — v1.1: адверсариальное ревью (3 линзы, 23 находки → 9 подтверждено).
  Исправлено: ложная атрибуция «goal, not procedure» для Opus 4.8 (это указание для
  Fable 5; Opus 4.8 следует инструкциям буквально — scope указывать явно); у scout
  отобран Bash (read-only теперь структурный); добавлена дисциплина рабочего дерева
  для параллельных builder'ов; протокол NEEDS_CLARIFICATION/stop-and-report включён
  в лестницу эскалации; уточнены цитаты источников (15×/4× стоимость токенов,
  «different model» для судьи, автоматическая инжекция CLAUDE.md субагентам).
- 2026-07-03 — v1.0: первая версия (скилл + 4 агента + 2 справочника).
