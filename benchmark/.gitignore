__pycache__/
*.pyc
.pytest_cache/

# Ключи античит-задач — только локально: репозиторий публичен (замечено
# 2026-07-23), закоммиченный ключ = слитый ключ. Ключи волны-0 уже в истории
# и на GitHub (git не выгружает уже отслеживаемое из-за ignore-правила) — та
# волна запечатана; это правило защищает волны, начиная со следующей
# (architect/t4–t5, recon/t1–t3 и далее).
tasks/*/*/hidden/
